import 'package:flutter/material.dart';

class TangsInterests extends StatelessWidget {
  const TangsInterests({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("hello"),
    );
  }
}
